{
  "buildCommand": "npm install && npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/api/claude", "destination": "/claude.js" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
